@model IEnumerable<Harjoittelu.Models.RFIDTagViewModel>

@{
    ViewData["Title"] = "RFID Tags";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Rfid_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rfid_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Serial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Tag_Id">Assign/Edit</a> |
                    @*<a asp-action="Details" asp-route-id="@item.Tag_Id">Details</a> |*@
                <a asp-action="Delete" asp-route-id="@item.Tag_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
